apiVersion: v1
kind: Service
metadata:
  name: daily8-redis
spec:
  type: ClusterIP

  selector:
    app.kubernetes.io/name: daily8
    app.kubernetes.io/component: daily8-redis

  ports:
    - name: redis
      port: 6379
      targetPort: redis
      protocol: TCP
